# U8 copy audit: findings (2026-09-25)

| File:line | What is there | Rule | Fix | Status |
|---|---|---|---|---|
| site/insights.html:76 | Departments "Show as table" prints the sentinel `OTHER` in the Department column | (a) all-caps label | `esc(r.dept === "OTHER" ? "Other departments" : r.dept)`, as the dot plot on :75 does | fixed, d3fe89d |
| site/course.html:53 | Related-courses list prints "(n of its own cases, OTHER estimate)" | (a) all-caps label | `esc(s.pooled === "OTHER" ? "other departments" : s.pooled)` | fixed, d3fe89d |
| site/courses.html:122 | Table sub-line prints "· OTHER estimate" at estimate_level dept | (a) all-caps label | `esc(x.s.pooled === "OTHER" ? "other departments" : x.s.pooled)` | fixed, d3fe89d |
| site/assets/site.js:194 | pooledTag's title reads "estimate uses the whole OTHER department" | (a) all-caps label | needs a pass: the sentence needs rewording for the group, with one label helper shared by :194, :201, :202 and index.html:108 | deferred |
| site/assets/site.js:201 | pooledSentence reads "Department estimate: the whole OTHER department at these positions." | (a) all-caps label | needs a pass (same helper) | deferred |
| site/assets/site.js:202 | pooledSentence reads "Pooled over the whole OTHER department at these positions." | (a) all-caps label | needs a pass (same helper) | deferred |
| site/index.html:108 | Lookup fallback reads "Showing the estimate for the whole OTHER department at ..." | (a) all-caps label | needs a pass (same helper) | deferred |
| site/course.html:65 | The h1 carries `<span class="tag">` with the level ("lower division"): smaller, muted and boxed inside the headline | (b) styled fragment in a headline | needs a pass: move the tag out of the h1 to the muted line on :64; a structure change, Oliver's eye | deferred |

Findings: 8. Fixed: 3. Deferred: 5.
