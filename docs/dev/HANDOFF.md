# Handoff: Berkeley Waitlist Odds

Written 2026-09-19 23:20 UTC at the end of the first two working sessions; updated 2026-09-20 00:10 UTC by the third session (its changes are marked "session 3" below) and 2026-09-20 03:30 UTC by the fourth (the backfill and backtest section below). Read this first in a new session, then `CLAUDE.md` (the `Current step` line) and `docs/dev/FINISH_PLAN_WAITLIST.md` (the step checklist and tracker at the bottom).

## Where everything is

| Thing | Location |
| --- | --- |
| Repo (public) | https://github.com/oliver139-chinesemole/berkeley-waitlist-odds, local checkout at `/Users/oliverguo/berkeley-waitlist-odds` (`main`, about 53 commits, CI green, 295 tests in 22 files) |
| Live site | https://oliver139-chinesemole.github.io/berkeley-waitlist-odds/ (lookup page, methodology page; shows a no-data state until Spring 2027 waitlists clear) |
| Data | `data` branch of the repo: `snapshots/date=YYYY-MM-DD/HHMM-{baseline,delta}.parquet`, `catalog/<term_id>/catalog.json`, `catalog/site.json`, `status.json`. Local clone at `./data-branch` (gitignored). 12 snapshots so far, all Fall 2026 test data. |
| Environment | `source .venv/bin/activate` (Python 3.12.5, pinned in requirements.txt). `gh` is logged in as oliver139-chinesemole; the `origin` remote uses the SSH alias `github-chinesemole`. |
| Plan and design docs | `docs/dev/FINISH_PLAN_WAITLIST.md` (roadmap), `docs/SPEC.md` (original spec), `docs/PHASE0.md` (data-route evidence), `docs/DESIGN_A2.md` (scraper contract, sections 11 to 15 override earlier ones), `docs/DESIGN_A4.md` (flows), `docs/DESIGN_A5.md` (survival analysis), `docs/DESIGN_A6.md` (site), `docs/ASSUMPTIONS.md` (the documented assumptions with validation numbers) |
| Operations | `docs/RUNBOOK.md` (go-live state, how a run works, what to do when it fails, Sunday check, site and weekly analysis), `docs/DATA_LOG.md` (every outage, switch and decision with UTC times; step A4 reads it as censoring rules), `CLAIMS.md` (every resume claim with its check command and the last measured result) |
| Memory | `~/.claude/projects/-Users-oliverguo/memory/project_berkeley_waitlist_odds.md` |

## What has been done (steps A0 to A6 of the plan)

**A0, dates and access.** Spring 2027 dates confirmed from the registrar's Google Calendar feed: Schedule of Classes publishes Oct 4, 2026; Phase 1 opens Mon Oct 26; Phase 2 Nov 23; adjustment period Jan 11, 2027; instruction Jan 19; last automatic waitlist run Feb 5; add/drop deadline Feb 10. The SIS Class API was requested and **denied: API Central does not grant access to students**. The `sis_api` adapter stays in the code in case a sponsor ever opens it.

**A1, data route (docs/PHASE0.md).** classes.berkeley.edu section pages embed the SIS enrollment JSON (enrolled, capacity, waitlisted, waitlist capacity, reserved seats) without login and are the primary source. Findings that shaped everything after:
- The site returns 403 to Python `requests` and `httpx` because they advertise ALPN `http/1.1` alone; the stdlib `urllib` client passes, from macOS and from GitHub's Linux runners. Do not switch clients and do not impersonate a browser.
- The site's robots.txt disallows `/search/`, so the listing is never crawled. Discovery uses the site's own `/rss.xml` (10 newest nodes with ids) and `/node/<id>` enumeration above a watermark kept in `catalog/site.json`. Spring 2027 pages will be found this way after Oct 4.
- Berkeleytime's public GraphQL gateway works from a laptop (cross-checks agreed 20 of 20) but is blocked by Cloudflare for GitHub's whole network. It is a local cross-check only (`probe/crosscheck_berkeleytime.py`).

**A2, scraper and storage.** `scraper/` package: pinned pyarrow schema, append-only Parquet per run (daily baseline plus change-only deltas with tombstones), rebuild of the full panel, gap report, three source adapters, polite HTTP client (1 request/s, retries with Retry-After, deadline-aware, bounded bodies). Priority list in `config/priority_courses.txt` (ordered by importance; about 917 sections every run) plus 12 rotating shards for the remaining live primary sections (about 3,660 in total), 1,380-second budget per run. Snapshot files store run metadata once in the footer with zstd (about 15 KB per delta); the data branch grows about 1 MB a day.

**A3, live operation.** GitHub Actions `scrape.yml` runs every 30 minutes. GitHub's cron fired only every 2 to 4 hours for this new repository, so the workflow keeps its own cadence: the last step of each run sleeps to the next :07/:37 slot and dispatches its successor with the built-in token (pushes made with that token never trigger workflows, but `workflow_dispatch` does). It stands down when another run is active or pending. `heartbeat.yml` restarts a dead chain; the chain dispatches `monitor.yml` once a day after 15:00 UTC; `monitor.yml` opens a "Scraper gap alert" issue when fewer than 40 runs land or a gap exceeds 90 minutes or the median missing share exceeds 0.5. Verified 2026-09-19: chain links at 20:07, 20:37, 21:07, 21:37, 22:07, 22:37 all on the slot. A restricted node (530942, HTTP 403) had pinned the discovery watermark for a few runs; fixed in the last commit; the watermark finishes catching up around 00:07 UTC on Sep 20, after which every run should observe its whole selection.

**A4, flow reconstruction (validated).** `analysis/flows.py` splits count changes between consecutive observations into admits, waitlist joins, waitlist drops, direct enrolments and enrolled drops with an `ambiguous` flag and censoring from the data log; `analysis/positions.py` gives time to clear for a virtual waitlister under optimistic, central and pessimistic drop scenarios. `analysis/synthetic.py` simulates real students; at 30-minute sampling the reconstruction recovers admits within 9%, joins within 4%, single-event intervals exactly, and the central time-to-clear has a 25-minute median error (docs/ASSUMPTIONS.md section 7). A labelled figure of this is in the README and on the methodology page.

**A5, survival analysis (code complete, no real data yet).** `analysis/calendar.py` (phases per term), `cohort.py` (virtual waitlisters with covariates), `survival.py` (Kaplan-Meier by position bucket, level, department and phase; log-rank; Cox PH with cluster-robust errors, PH test and stratified refit; sensitivity across scenarios; out-of-sample scoring on Phase 2 joins with concordance, Brier and decile calibration), `profile.py` (data-quality profile), `export.py` (site JSON with pooling below 30 cases), `figures.py`, `run.py`. `make analysis TERM=2272` reproduces everything from the data branch. On Fall 2026 test data the cohort has no clearing events, so the report says so; the pipeline is exercised end to end on simulated data in `tests/test_run.py`.

**A6, site (v1 live).** `site/index.html` and `site/methodology.html`, deployed by `pages.yml`. `analysis.yml` runs every Sunday 15:23 UTC (or on dispatch), commits `site/data/*.json` and `reports/` to `main`, and dispatches the redeploy. Verified end to end on 2026-09-19. Session 3: `tests/test_site.py` renders the page's own script under node with a stub document and a file-backed `fetch` (`tests/site/harness.js`; node comes from PATH or `~/.nvm/versions/node/*`, the tests skip without it) and checks the empty state, the counts from `meta.json`, the full state, a lookup, the pooled tag, the missing-course and missing-bucket messages and the `?course=&position=` link. Writing it exposed a live bug: `analysis/run.py` put the per-scenario cohort sizes under `meta["cohort_rows"]`, so the no-data state read "704 sections, [object Object] hypothetical joiners"; fixed (the dict is now `cohort_rows_by_scenario`) and `analysis.yml` was dispatched to regenerate the live JSON.

**Claims.** `CLAIMS.md` rows for the test suite, the cross-check, the public repo, the flow reconstruction, the analysis command and the site are measured. Session 3 re-measured the cadence rows at 23:48Z on Sep 19: 13 runs in the 24 h window (which still holds the cron-only morning), but every slot since the chain started at 19:41Z (8 runs, largest gap 30.1 min, share under 45 min 1.0). The 24 h window can first pass after 19:41Z on Sep 20; re-run rows 5 to 7 then. The `claims-audit` skill at `~/.claude/skills/claims-audit/` re-runs every row.

## Session 4 (2026-09-20, 01:00Z to 03:30Z): Fall 2026 backfill and the backtests

Branch `backfill-backtest` (docs/dev/BACKFILL_BACKTEST.md is the plan; its checkboxes carry what each step found). Summary:

- **Backfill** (`analysis/backfill.py`, `make backfill-pilot` / `make backfill`): Berkeleytime's `GetEnrollment` run-length history becomes the panel `interval_flows` reads; within-segment intervals are observed stillness, crossings wider than 180 min are that recorder's gaps and are censored; `gap_report.csv` per day; selection never reads today's counts and is a seeded permutation (a pilot is a prefix of the full pull); resumable gzip cache under `backfill/` (gitignored, Berkeleytime's data, labelled `berkeleytime_history`). The gateway rejects the second recorded `GetEnrollment` op; the recorded ops are tried in order.
- **Pilot pull** (296 sections): Berkeleytime was dark for every section from 2026-08-19 22:45Z to 2026-09-01 18:30Z (end of adjustment and first week of instruction), plus May 22, May 28 and Jul 18 to 19; its poll spacing at changes was 15 min from July but 45 to 130 min in March to June (hence the 180-min gap rule, not 45). Spring 2026 histories exist (20 of 20 probed, from Phase 1 start).
- **Backtest** (`analysis/backtest.py`, `make backtest`): temporal (training rows censored at the split), grouped-by-course and cross-term splits; bucket KM, pooled course KM, the literal site number and Cox scored at each row's own horizon with IPCW, weighted AUC, calibration and a section bootstrap. Observability rule: a row is scored only at horizons inside its `follow_up_days` window (a joiner who cleared before a gap is seen, one who did not is not, so no weighting recovers negatives); on Fall 2026 the deadline and instruction horizons are unobservable for every joiner before the hole, so the term is scored at 14- and 28-day horizons. Pilot results are in CLAIMS.md (Cox +0.037 over the bucket baseline within a regime; every Phase 1 model under-predicts Phase 2; course cells add nothing over buckets).
- **Cohort**: `joinable` rule (a queue exists or the section is full; 44% of the earlier test rows were impossible joiners), positions 1 to 100, numpy position walk identical to `time_to_clear` and more than 100 times faster, `follow_up_days` column. The whole suite now runs in about 70 s.
- **Site** (`site/index.html`, `analysis/export.py`): the curve is exported out to each cell's reach with a section-bootstrap band and section counts; the page reads it at the asker's own days left before the last automatic waitlist run (headline) and before instruction (second line), notes a floor past the reach, becomes a look-back after the deadline, labels the Berkeleytime source, and accepts `?today=` (the tests pin it). `p_clear_by_instruction` is the report's number only.
- **Calendar**: `SPRING_2026`; the last automatic run for terms without an explicit row is the last day to add without a fee (FA26 Sep 11, SP26 Feb 6), because Spring 2027's registrar row puts them on the same day.
- Docs: DESIGN_A5 (section 9), DESIGN_A6, methodology page, README, DATA_LOG (pilot decision row), CLAIMS (three rows).

### Session 4, second half (2026-09-20, 18:20Z to 20:00Z): the full pull installed

- Full Fall 2026 pull: 6,016 sections (Berkeleytime's catalog has no MATH 1A or 1B), 402,573 segments; fetch ran 02:44Z to 09:42Z with the laptop asleep for part of it (keep `caffeinate -i` on long pulls). The Fall analysis then stalled for nine hours in lifelines' cluster-robust Cox: its score residual is an O(n²) row loop. Fixed by rounding durations to 0.01 day (tied times batch) and `analysis/coxfast.py`, an O(n) cumulative-sum form of the same residual held to the original to 1e-8; the full 475,079-row design now fits in 14 s. PH test on a 50,000-row diagnostic refit. `--cox-max-rows` exists but defaults to no cap.
- Backtests on the full pull (CLAIMS.md rows): course cells no better than the bucket baseline, so `export_site_tables` serves department curves by default (`level="dept"`, `meta.estimate_level`); the page says "department estimate" with the course's own counts. Across the Phase 1 to Phase 2 shift even department curves lose to the bucket alone (−0.017) and Cox extrapolates badly (−0.290); within a regime Cox adds +0.034. The Spring 2026 to Fall 2026 cross-term run (in progress) decides whether departments transfer across terms.
- Installed: `site/data` (2,050 courses, `berkeleytime_history`, `estimate_level` dept), `reports/report_2268_berkeleytime.md`, `reports/figures_2268_berkeleytime/`, `reports/backtest_2268/`, data-log row, CLAIMS rows (backfill, analysis, backtest, headline, cadence on the first full chain day: 46 runs, 97.8% of gaps at or under 45 min). Install commit a7240f4 (PR #5) is the pre-registration point.
- Also: `analysis/priority_from_flows.py` (B5 tool), `analysis/backtest_summary.py`, LICENSE, `docs/dev/`, `docs/dev/STATUS.md`, the weekly workflow's site-data guard (verified: the 18:15Z Sunday run committed only the report), issue #1 closed after the 18:11Z monitor run. Site v3 is being built by another session in a worktree (PR #4) and rebases onto the install commit.

### Session 4, third part (2026-09-20 20:00Z to 2026-09-21 00:30Z): Spring 2026 and the cross-term test

- Spring 2026 pulled under `caffeinate` (6,131 sections, 19:29Z to 23:16Z), built and analysed (531,568 virtual waitlisters, 299,826 clearings; 2 of 202 days dark; adjustment and instruction phases observed). Report at `reports/report_2262_berkeleytime.md`.
- Cross-term backtests (fit on Spring 2026, score Fall 2026; `reports/backtest_2268/cross_term_*`): course +0.0095, dept +0.0051 over bucket at 14 days, both intervals clear of zero; larger at 28 days and at the deadline; Cox and the v1 site rule worse than bucket. The department-level default lasted one evening: `DEFAULT_LEVEL` is `course` again, `site/data` re-exported, methodology reworded, CLAIMS cross-term row added, data-log row for the Spring pull.
- Open decision for Oliver: serve Spring 2026 (complete, same season) instead of or alongside Fall 2026; see docs/dev/STATUS.md section 3.
- Unexplained: a second `analysis.run --backfill-dir backfill/2268 --estimate-level course` run, started while the test suite was running, sat in the Cox/PH stage for 45 minutes at full CPU and was killed; `fit_cox` on the same cohort in isolation took 45 s under load. `site/data` was then written directly with `export_site_tables(level="course")` from the installed cohort and the analysis's own meta (the scratchpad script `export_course.py`; 20 minutes, most of it the course-cell bootstraps). If it recurs, sample the process before killing it.

## Session 6 (2026-09-23 21:00Z to 2026-09-24 09:00Z): the master plan, the collection fixes, the Spring 2026 source

Read `docs/dev/MASTER_PLAN.md` first from now on; its section 12 records what this session verified against the repo. Everything below is in draft PRs for Oliver's review, in two stacks: `main` ← #8 ← #9 ← #10 ← #12, and `main` ← #4 (site v3) ← #11. `docs/DATA_LOG.md`, `CLAIMS.md`, `CLAUDE.md` and `docs/dev/STATUS.md` will conflict trivially between the stacks at merge time (both append or edit the same lines): keep both sides.

- **Diagnosis (PR #8).** classes.berkeley.edu slowed from 2026-09-21 14:37Z (2 to 5 s per page, 20 s read timeouts). The client spaces request starts 1 s apart whatever the worker count, so with 2 workers throughput fell to 2/latency; ten runs exceeded `--max-missing-share 0.5` and wrote nothing (issue #7). scrape.yml now passes `--min-interval-s 0.5 --max-concurrency 4` (DESIGN_A2 section 16; CLI defaults unchanged). Same PR: analysis.yml replaces `site/data` only for term 2272; the Fall 2026 own-data cohort had 209 post-deadline clearings and would have displaced the Berkeleytime backfill on Sunday Sep 27.
- **Priority list (PR #9).** `config/priority_courses.txt` is generated: `analysis/priority_from_flows.py --top-sections 1500 --min-course-joins 100 --catalog ...` over both cycles gives 1,013 exact course patterns (14 by course total, the reading-and-composition courses among them), 1,213 live sections in 661 courses per run, 89.2% of reconstructed joins and 51.0% of queued section-terms (hand list: 47.7% and 29.7%). `--catalog` reports what a run fetches, which is where the live numbers in the data log come from. MATH 1A/1B no longer exist: Fall 2026 calculus is MATH 51/52.
- **Catalog titles (PR #10).** `catalog.json` entries carry `title` and `instructors` read off the section page (DESIGN_A2 section 18), zero extra requests; both write paths keep learned values when a page lacks the elements.
- **Spring 2026 source (PR #11, on site-v3).** The page serves Spring 2026 for Spring 2027 (P4): Fall 2026 gives own curves to courses carrying 62.0% of Spring 2026's joins, Spring 2026 99.5%; Phase 1 joiners at positions 1 to 5 got in within 14 days 66% of the time in Spring 2026 against 48% in Fall 2026. Numbers in the data-log decision row. Fall 2026 stays frozen at a7240f4 for the pre-registered test.
- **Reviews.** Every PR was reviewed by a subagent ("with fixes" each time; fixed). Lesson: the worktree guard is session-wide, so switching worktrees while a reviewer runs breaks its shell.
- **This PR (#12).** `PrioritySpec.rank` is a dict lookup for exact patterns (it scanned a thousand patterns per section, 6.5 s a run); this handoff section; plan rows brought up to date (P4 decided, X7 already done on site-v3, the off-season home page moot now that v3 counts down to Spring 2027).

After the merges, in order: a data-log note row for the first run at the new rate and with the new `priority_sha` (`n_observed`, `sweep_seconds`); close #7 after a clean 24 h and re-measure CLAIMS rows 7 to 9; Oct 4 term switch (RUNBOOK step 7, read `selected=`); `pages.yml` after #11 and the CLAIMS check against the live `meta.json`; count catalog entries with empty titles per component after about twelve runs; Q4 `titles.json` from the catalog. Waiting for #4: search Q1 to Q3, the section board S1 to S3, design U1 to U4.

## Session 7 (2026-09-24 20:10Z to 2026-09-25 07:28Z, unattended until 00:10Z): Track B of `docs/dev/NEXT_2026-09-24.md`

**Note for Oliver, first.**

1. **The scraper's stop rule fired twice tonight, and the rate flags are your call.** classes.berkeley.edu's latency of Sep 21 to 23 is back in episodes of about fifteen minutes with clean runs between them. The 19:07Z run observed 1,255 of 1,453 sections (196 of the 198 missing in the shard tail, 2 on the priority list; 33 timeouts, 8 HTTP 504 pauses); the 22:37Z run observed 1,443 of 1,445 but swept in 1,342.8 s against the 1,380 s budget (45 timeouts, 8 504s), and the 23:07Z run 1,428 with 1 missing in 1,350.9 s; the 23:37Z run was clean again (1,434, 0 missing, 903 s). Every run since 09:37Z wrote a snapshot, no 429 or 403 appeared, so RUNBOOK item 9's rollback trigger has not fired and the flags were not touched. The runs have `note` rows in `docs/DATA_LOG.md` (PR #15). The arithmetic (DESIGN_A2 section 16): at four in flight the budget covers the 1,450-page selection up to about 3.8 s a page and the 1,213-page priority list up to about 4.5 s; the slow runs were 3.7 s a page. `--max-concurrency 6` in `scrape.yml` would hold the selection up to about 5.7 s a page at the same 2-per-second ceiling. If the episodes lengthen, that is the change to make; if a 429 or 403 appears, item 9 applies instead. **Overnight (2026-09-25) two more episodes: the 00:37Z run lost 243 sections, 30 of them on the priority list, because the episode began with the run; see the second continuation below.**
2. **Open PRs, in merge order.** #15 (this session's docs, against `main`, any time). Then #4 (site v3), then #11 (Spring 2026 source, on #4), then `gh workflow run pages.yml` and the curl checks in `SITE_HANDOFF.md` section 2. Then the two site PRs stacked on `site-v3`, each rebased onto `main` after #4: #17 (search: Q1 to Q3, Q8), #16 (design: U3, U4), #19 (the section-board fixture and contract) and #20 (the board itself, on #19). PR #18 adds the agent definitions.
3. **Decisions only you can take, all written at the top of PR #16's body:** the interface face is Atkinson Hyperlegible Next (the named Atkinson Hyperlegible ships no variable font and its two statics were 68 KB; the alternative is to merge U3 alone); the keyboard focus ring stays gold as its own chrome token; `--rust` colours error text and the pooled tag until U2 gives it seats held back. And on PR #17: the subject names come from the Internet Archive's 2025-05-27 snapshot of the Academic Guide's subject list (the Guide now redirects to a JavaScript catalogue), with four codes left unnamed.
4. **Needs your phone or browser.** The v3 pages on #4 (light and dark, `?today=2026-09-15` for the look-back) and, once rebased onto `main`, #16's palette and fonts on a real screen: the smoke screenshots are in each push's `site-smoke` artifact (run 36074407105 for #16's head).

**What happened, in order.**

- **Pre-flight.** The main checkout was on `backfill-full` and was left alone (peers idle on it); everything was read from `origin/main` and done in worktrees (`.claude/worktrees/s7-orchestrator` and one per agent). #4 and #11 open with green checks; no merge authorisation, so Track B. Issue #7 was already closed by the #8 merge at 09:13Z. `claude` is not on this shell's PATH (as in session 6) and the Agent tool carries no `effort` field, so the agents ran on the `opus` alias at default effort; the per-agent levels MASTER_PLAN section 10 wants need `.claude/agents/*.md` definitions, which do not exist yet. The 24 h gap report at 20:25Z: 47 runs, largest gap 31.1 min, share of gaps at or under 45 min 1.0, median missing share 0.0157.
- **Stop rule, first trigger (19:07Z snapshot, missing share 0.136).** Diagnosed from the run log, the footer and the catalog (item 1 above; DATA_LOG row). Ruling: site work continued, because the priority list was 2 short, the neighbours were complete and every 24 h threshold held.
- **B1, done.** `site/methodology.html` on `site-v3` (commit ea61bf4, 23 site tests green, #4's checks re-ran green): the request-rate sentence names no number and links the data log; "a fixed priority list" became "a priority list generated from the previous cycles' reconstructed waitlist joins".
- **B2, search (draft PR #17, branch `q1-q3-search`, five commits).** Q1: `config/subject_names.json` generated by `scripts/subject_names.py` (133 of 137 subjects in `index.json` named; DISSTD, ENERES, MBN and QTP under `_todo`; provenance as `_source` and `_fetched` keys, `--check` wired into the tests), `config/course_nicknames.json` hand-written and only read by the generator. Q2 and Q3: `BWO.matchCourse` in `site/assets/site.js` tries exact, alias, full name, nickname, filler-stripped, split-suffix and bare-number routes before a Damerau-Levenshtein pass on the subject token alone (≤ 1 up to five characters, ≤ 2 above, ties by joins; numbers never fuzzed); `findCourse` is `matchCourse().row`, so every caller keeps working. Q5's "Showing X for *query*" on fuzzy hits and the three nearest candidates under the miss message, both on the index page. Q8: `tests/fixtures/course_queries.csv`, 176 rows (every row of the plan's table, 24 misses), run by `tests/test_site.py` and by `node tests/site/queries.mjs` through the same harness, plus a test that the fixture's layer set equals `BWO.LAYERS`. One DESIGN_A6 subsection. Review: spec compliant; one Important finding (the fuzzy pass ran before the cheaper variants, so `dis 100` became DATA C100 by typo-correction) fixed in round 1 with four small items (a fixture row that a data refresh could flip, an eager `nearest` and a new throw on a half-loaded index, `LAYERS` without `miss`, a duplicate comparator); the scoped re-review found every finding addressed with no new breakage (three nits deferred: a redundant layer-set literal beside the new `BWO.LAYERS` comparison in `tests/test_site.py`; nothing constrains where the fixture's `*` layer-only key may be used; the fixture shape test now needs node; and, outside the fix, `searchCourses` is still unguarded on a half-loaded index and `relatedCourses` keeps a third inline comparator). Full suite 368 passed, CI green. Deferred to the next search pass: no Showing line on `course.html`; comma-suffixed Guide names ("Business Administration, Undergraduate", "Art, History of") unreachable by the name route; no floor on a partial Guide parse in the generator; a `certifi` import against the script's "stdlib only" docstring; `MANUAL` overrides consulted only for codes the Guide omits; the provenance test accepts any https host and records the run date rather than the snapshot's.
- **B3, design (draft PR #16, branch `u3-u4-design`, six commits).** U3: the seven tokens per scheme with the plan's exact values, every other colour derived, no literal colour outside the two `:root` blocks, gold on the "you" bucket and chip, the two ticking key dates, the chart's two date marks and their legend key, and nothing else (the focus ring is its own chrome token at the same value; the gold grep is pasted in the PR body). U4: Source Serif 4 (headlines and the big figure; weight axis kept, optical size pinned at 20) and Atkinson Hyperlegible Next (interface), OFL files committed, subset with pyftsubset to 36,460 bytes of woff2 (56 KB by `du` with the licences), `font-display: swap`, system fallback stacks, the eight-step scale as `--fs-1` to `--fs-8`, `tabular-nums lining-nums` on every figure including `dl.kv dd` and the legends. Chart labels keep their SVG sizes (35b3a74). Review: values, subsets, axes, byte counts and cross-file references all verified from the files; three Important findings (the gold focus ring against the grep proof, `--rust` documented as "seats held back" while colouring errors and the pooled tag, the substituted face) resolved in two fix rounds as documentation and a "Decisions for Oliver" section, no pixel change except the two new tabular selectors; the scoped re-review confirmed each fix from the files (the second round was two comment lines, verified by the orchestrator's own read of the diff). Local smoke 36 renders with zero axe violations in both schemes, CI green. Deferred: `--tint` identical in both schemes; most secondary prose collapsed onto `--fs-1` (12.8px, plan-mandated, your eye); the `--s1` curve at about 2.2:1 on white (pre-existing, belongs to U7's ramp pass).
- **Stop rule, second trigger (22:37Z sweep 1,342.8 s, found 23:11Z).** Ruling: the two implementers already two hours in finished and were reviewed; B4 was not dispatched; nothing else site-side started; the rate flags stayed untouched.
- **B4, held at this point (done later as PR #19; see the continuation).** Its brief is at `.claude/worktrees/s7-orchestrator/.superpowers/sdd/NEXT_2026-09-24/task-B4-brief.md` on this machine (git-ignored, not committed; the agent worktrees under `.claude/worktrees/agent-*` from this session can be removed with `git worktree remove`); the substance follows. Deliverables: `tests/fixtures/latest_sample.json` in `scraper/live.py`'s shape plus `reserved_count` and `open_reserved`, for COMPSCI 61A one section in each of four states (open: 20 of 30, waitlist 0, "10 seats open, anyone can take them"; waitlist: 30 of 30, 5 of 10 waiting; full: 30 of 30, no waitlist; full and reserved: 26 of 30 with `reserved_count` 10 and `open_reserved` 4, "4 seats open, all reserved"), a fifth open row with 2 seats, one row observed seven hours before the pinned now, and two sections of another course; a `HARNESS_LIVE_FILE` hook in `tests/site/harness.js`'s `fetchStub` serving the course page's absolute live URL from a file; a new `tests/test_site_live.py` with one passing test (the block renders one row per section as today, absent without the file) and one `xfail(strict=True)` test pinning A3's contract (seats open, the reserved line in the plan's wording, "read N min ago" per row, `status-open|waitlist|full|reserved` classes and no red or green, a warning containing "hours" for the stale row, no "your position" anywhere). Two facts the A3 implementer needs: `scraper/live.py` on `site-v3` does **not** emit `reserved_count` or `open_reserved` (MASTER_PLAN 3.2 says it does; `scraper/schema.py` has both, `live.py`'s `COLUMNS` do not), and its selection is full-or-waitlisted sections only, so A3's data-side change is two columns plus the selection rule.
- **PR #15.** Two DATA_LOG rows, the plan file at `docs/dev/NEXT_2026-09-24.md`, this entry, the `Current step` line, the `SITE_HANDOFF.md` rows.

**Rulings made on your behalf** (each with what it costs if wrong): (1) continue after the first stop-rule trigger — site PRs opened during a wobble you would rather have watched; (2) Track B — none; (3) the plan file committed at its suggested home — one file to delete; (4) one docs PR for the note rows, the plan file and the handoff — the rows wait for the handoff's review; (5) agents on the `opus` alias without per-agent effort — agents ran at default effort; (6) B2 and B3 in parallel, B4 after B2 — a rebase between stacked PRs; (7) B1 pushed straight to `site-v3` as the plan says — none; (8) after the second trigger, finish and review the running items, start nothing new — B4 waits a session, or two draft PRs to close if you would rather have had them abandoned; (9) the Internet Archive snapshot of the Guide as the subject-name source — regenerate from a source you prefer, the generator is a script; (10) four subjects unnamed, the test asserting the top 40 — four names to type; (11) subagent commits keep their own model in the `Co-Authored-By` trailer (Opus 5) while the orchestrator's carry Fable 5.1 — nine commit messages to reword if you want one line; (12) to (16) the implementers' disclosed deviations (a trailing `R` joining the number; Atkinson Hyperlegible Next; the optical-size pin; the gold focus ring) left to the reviewers and then to you, provisionally accepted — each is one file or one line to revert; (17) and (19) five one-line minors joined the fix rounds because no final review runs this session — a longer round; (18) the reviewer's design findings answered with documentation and a decisions section rather than a code reversal — you revert if you disagree; (20) `site/index.html`'s two small hunks in #17 are within the brief — none; (21) the two-line comment fix in #16 verified by the orchestrator's own read of the diff rather than a re-review seat — a wrong comment line.

**Next session, in order.** The dated items in the plan's section 5: re-measure the three CLAIMS scraper rows from `python -m scraper.gaps --data-root ./data-branch --term-id 2268 --hours 24` after 2026-09-25T09:37Z (issue #7 is already closed; correct the sweep-time row's "1200 s budget" to 1,380). Then, once #4 and #11 are merged and `pages.yml` has run: A1's checks, then the first scrape run after #4 writes `live/latest.json` and the board on `course.html` (PR #20) shows real counts, then the rest of SITE_HANDOFF section 6. Oct 4: term switch (RUNBOOK step 7; read `selected=` against the budget with the latency arithmetic in item 1 in mind). Before any of it, look at the last six snapshots and the sweep times: the plan's section 3 pre-flight now has a real baseline (0 to 2 missing, 850 to 1,100 s on a clean run, 1,350 s in an episode).

**Continued at 00:10Z on 2026-09-25 (Oliver: "continue working").** Read as the go-ahead for the one item the stop rule had held back, under the same constraints (no rate-flag change, no merges); the 23:37Z run was clean first (1,434 observed, 0 missing, 903 s).

- **B4, done (draft PR #19, branch `s1-s3-fixture`, stacked on `site-v3`).** `tests/fixtures/latest_sample.json` in `scraper/live.py`'s shape plus the two columns A3 must add (`reserved_count`, `open_reserved`): COMPSCI 61A in the four states (open, waitlist, full, full and reserved), a second open row with 2 seats, one row observed seven hours before the pinned now, two sections of another course. A `HARNESS_LIVE_FILE` hook in `tests/site/harness.js`'s `fetchStub` serves the course page's absolute live URL from a file (a 404 as before when unset). A new `tests/test_site_live.py`: one passing test (the block renders one row per section as today; absent without the file) and one `xfail(strict=True)` test pinning A3's contract (seats open, the reserved line in the plan's wording, "read N min ago", the `status-open|waitlist|full|reserved` ramp with no red or green, a warning containing "hours" on the stale row, no "your position" anywhere), which errors the moment A3 makes it pass. Reviewed: spec compliant and approved, the xfail confirmed by the reviewer to fail on A3's missing content rather than on setup; one short round for four minors (the PR body now names the warning class words `warn` or `stale`, the one-row-per-section coupling, the `read 7 hours ago` wording for the stale row and that the block stays a table; the harness matches the literal live URL only and fails loudly on an unparseable clock pin). CI green. One sentence in that body still says `site.css` has a `--warn` token: true on `site-v3`, but after #16 the token is `--rust`. So A3 is the 30-minute job the plan wanted: add the two columns and the selection rule to `live.py`, build the rows, drop the xfail marker.
- **PR #18, agent definitions.** The seven `.claude/agents/*.md` files MASTER_PLAN section 10.2 lists (scraper-ops and analysis at `max`, site-builder, design and reviewer at `xhigh`, search at `high`, explore at `low`, all on the `opus` alias, implementers and the reviewer with `isolation: worktree`), each carrying section 8's rules for its files and the worktree-guard notes; plus `.gitignore` entries for `.claude/worktrees/`, `.claude/settings.local.json` and `.superpowers/`. The docs confirm `effort` as a frontmatter field and that the directory must exist at session start, so the next session is the first that can use them. Two rulings: (22) "continue working" read as authorisation for B4 — one PR to close if not; (23) the agent files as enabling work — one PR to close.

Merge order with these: #15 and #18 (docs and definitions, any time, independent), then #4, #11, `pages.yml`, then #17, #16, #19 and #20 rebased onto `main`.

**Continued again at 06:40Z on 2026-09-25 (Oliver: "keep working").**

- **Overnight, two more stop-rule triggers (DATA_LOG row 2026-09-25T00:37Z).** The 00:37Z run observed 1,183 of 1,426 sections: a fifteen-minute timeout episode (69 read timeouts, one HTTP 504) began with the run and starved the whole selection, so **30 of the 243 missing were on the priority list** (ANTHRO 121AC and 159, ARCH 207D, CHEM 103, CIVENG 100, CYPLAN C256, ESPM C126, GEOG 138, HUM 105, INDENG 145 and 150 among them), the first priority losses since PR #8's policy took effect. The cron run that took the 01:07Z slot was complete, every run to 05:37Z had 0 missing, the 06:07Z run lost one shard section in 1,381.5 s with 18 timeouts near its end, and the 06:38Z run was clean in 829.8 s. Four episodes in twelve hours (19:07Z, 22:43Z, 00:37Z, 06:26Z), each about fifteen minutes; the 24 h gap report still holds (47 runs, largest gap 31 min, no lost run); no 429 or 403. The one-line change, if you want it, is `--max-concurrency 4` to `6` in `scrape.yml` with a `decision` row naming you; it was put to you in the session and not taken by the assistant.
- **A3, done (draft PR #20, branch `s1-s3-board`, stacked on PR #19's branch).** `scraper/live.py` emits `reserved_count` and `open_reserved` and writes every section of any course that has at least one full or waitlisted section (`--all` unchanged; the file carries `only_active` false and a `selection` key); the course page's "Sections now" table gains seats open, the reserved line in the registrar's wording, `read N min ago` per row, a blue status ramp (`status-open|waitlist|full|reserved` from the `--s1` to `--s4` tokens) and a plain stale note inside rows older than six hours; DESIGN_A6 updated; PR #19's contract test passes with its marker removed. Reviewed: spec compliant and approved; the contract test came out of xfail with every assertion untouched, and the reviewer drove the null and boundary cases through the harness itself. Two short rounds: three sentences elsewhere that still described the old selection (DESIGN_A2, RUNBOOK with the measured size, the scrape.yml step comment), two null-display edges, a fixture row for the partly reserved case, then one DESIGN_A6 sentence and the fixture's selection key; the scoped re-review found every finding addressed with no new breakage. Ten commits, CI green. Deferred for a later pass: partial-null combinations still read as open; the em-dash branches have no automated test; the em-dash status cell has no accessible text; the six-column board at phone width and the darkest ramp step on the dark palette want your eye. Merge order with it: #19 then #20 onto `site-v3`, after #4. Once `live/latest.json` exists on the data branch (the first scrape run after #4 merges), the board shows real counts; until then the block stays hidden.

## Session 9 (2026-09-25, 18:10Z to 19:45Z, unattended): `docs/dev/NEXT_SITE_2026-09-25.md`, Track B

**Note for Oliver, first.**

1. **Open PRs, in merge order.** Docs and collection, any time: #15 (`s7-docs`, sessions 7 and 8) with the session 9 docs PR (`s9-docs`, stacked on `s7-docs`) on it (this entry, the renewed STATUS, the plan files, one data-log row), #18 (`.claude/agents` definitions), #21 (`claims-remeasure-2026-09-25`: the three scraper rows re-measured 24 h after the request policy, 47 runs, every gap under 45 min, 878 s sweep). Then the site: **#4 is mergeable again** (`main` a5b36df merged into `site-v3` as a3e0f21, four conflicts resolved as the plan said, plus the review's one-line fix 5325cbc), then #11 (Spring 2026 source, rebased), then `gh workflow run pages.yml` and the curl loop in SITE_HANDOFF section 2. Then the stack, each rebased today onto `site-v3` 5325cbc with green CI and proven to merge in this order in a throwaway worktree (388 tests, the query fixture 176 of 176 on the integrated Spring data, the board rendering, fonts 56K, smoke clean): #17 (search; it carries a "Rebase notes" commit 23ec3ec so its fixture and subject names hold on Spring 2026 data), #16 (palette and type), #19 (board fixture), #20 (the board, on #19). Then tonight's follow-ups, each reviewed by a subagent and approved, stacked where they belong: #24 (`search-followups` on #17, one fix round), #22 (`design-followups` on #16), #23 (`board-followups` on #20), #25 (`s6-crosslist` on #23), #26 (`c3-fallback-ladder` on `site-v3`, one fix round), #27 (`c2-status-line` on `site-v3`, one Important fixed and re-reviewed), #28 (`c6-titles-export` on `site-v3`; merge after #11 and rebase: `Makefile`, `tests/test_export.py` and `docs/RUNBOOK.md` conflict, resolutions in its body), #29 (`c6-title-search` on #24), #30 (`c5-dept-pages` on `site-v3`; reviewed and approved; one fix round put the department link on every course page), and #31 (`u8-copy-audit` on #22: the U8 copy audit, eight findings, three one-line fixes, five for you). Every site PR retargets to `main` after #4 merges. Nothing was merged tonight; no branch was deleted.
2. **Decisions only you can take.** (a) PR #16's three (Atkinson Hyperlegible Next, the gold focus ring as its own token, `--rust` on error text) and PR #22's two: `--s1 #5297E8` is 3.02:1 on white but 2.76:1 on the page background where charts and the board sit, and `--s1` against `--s2` is now 1.20:1, so the two lightest curves look alike; the values are in its body. (b) `--max-concurrency` 4 to 6: one more latency episode tonight (18:07Z: 1,294 s, 1 missing, four 504 pauses, about 35 timeouts, no 429 or 403), under the stop rule; flags untouched. (c) PR #17's substituted plan-table query (`molecular and cell biology 104` for `32`, which Spring 2026 does not offer) and whether the course page's "Showing X for" line should also fire for bare numbers (`?c=16a` quietly shows EECS 16A on Spring data). (d) Department pages live at `dept.html?subject=CODE`; a pretty `/dept/CODE` path needs exporter-written stubs. (e) PR #28's `titles.json` names 1,728 of 2,050 Fall courses; 23 of the missing are cross-listings whose title sits under the partner key, a Q4 coverage follow-up. (f) PR #29's stop-word list and surname rule (last word only: "Fernandez-Preston" and "Washington Jr" miss) are first cuts.
3. **Needs your phone or browser.** The v3 pages on #4 (light and dark, `?today=2026-09-15`); the section board on #20 and #23 on an iPhone with VoiceOver: both the board and the Courses table become grid rows below 38 rem, and WebKit has dropped table semantics for that pattern; the fix is explicit table roles in the markup for both tables, not done tonight (a Minor on #23). Every push's `site-smoke` artifact has screenshots; the department page adds four renders (40 in PR #30's smoke).
4. **Stop-rule readings.** 18:10Z: 47 runs in 24 h, largest gap 31.1 min, every gap under 45 min, last run 1,434 observed, 0 missing, 878 s. 18:40Z: the 18:07Z run swept in 1,294.2 s with 1 missing during a latency episode (four HTTP 504 pauses, about 35 timeouts, no 429 or 403; a `note` row is in the session 9 docs PR (`s9-docs`, stacked on `s7-docs`)). 19:27Z: the 19:07Z run 1,453 observed, 0 missing, 1,061.9 s; every run to then success. 19:37Z: unchanged (the 19:37Z run in progress, every earlier run success). No run was lost; the rate flags, the priority list and `scrape.yml` were not touched.

**What happened, in order.**

- **Pre-flight.** Main checkout on `backfill-full`, left alone. `.claude/agents/*.md` installed by hand from PR #18's branch; they do not resolve as agent types in a session that started without them, so every agent ran on the `opus` alias with its effort level stated in the brief's first line (the reviewers on `opus`, one scoped re-review on `sonnet`). Track B (#4 open, no authorisation line). Baseline 360 tests on `site-v3`. The `superpowers` skills were installed this time and used: using-superpowers, using-git-worktrees, brainstorming (proportionate; section 6 stood in for approval), writing-plans (four plan files in `docs/dev/plans/`), subagent-driven-development (ledgers in `.superpowers/sdd/`, briefs and review packages as files, a reviewer per task), claims-audit; the design and ux-copy reviews ran inside the implementers' briefs and their findings are in the PR bodies.
- **CLAIMS re-measure (PR #21)** from the exact commands in the rows, run from the main checkout because row 9's command uses `git -C`, which the worktree guard refuses.
- **B1.** `main` merged into `site-v3` as a3e0f21: the methodology keeps `site-v3`'s sentence; CLAIMS takes `main`'s Sep 23 re-measures (each cites the Sep 19/20 value as "Previous"), the test-suite row folded; CLAUDE.md and HANDOFF keep both sides. 375 tests, smoke 36 renders 0 problems, CI green, PR #4 MERGEABLE, comment posted. Reviewer: Approved, six Minors on one CLAIMS row, fixed as 5325cbc.
- **B2.** #11, #17, #16, #19, then #20 onto the rebased #19, each pushed with lease, CI green, bases unchanged. #11 needed four resolutions (its data-log row placed by date; methodology = B1's first paragraph plus #11's Spring paragraph, in both its commits; CLAUDE.md kept the base's line); the other four were clean. After 5325cbc the first three were re-rebased. Rebase notes appended to #11's and #19's bodies after the B2/B3 review asked.
- **B3.** Integration worktree `site-v3-integrated`: all five merged clean. The first run failed two tests, #17's Fall-built query fixture and subject names against #11's Spring index (twelve term-dependent rows, not two; the runner's tail had been cut). Fixed on #17, the later branch, by the search agent as 23ec3ec (rows that hold on both indexes, names covering both). Re-run: 388 passed, 41 site and live, 176 of 176 queries, board 7 rows, fonts 56K, smoke 36 renders 0 problems. Reviewer (range-diffs, bases, both runs): Approved, eight Minors (the two body notes above, the ledger's undercount, the leftover worktree).
- **C1.** Three agents in parallel, each reviewed: #24 (search follow-ups; approved; one fix round put the `*` convention on the two new bare rows and corrected DESIGN_A6), #23 (board follow-ups: partial-null rows are never "open", phone rows, em-dash tests; approved; VoiceOver concern yours), #22 (design follow-ups; approved; the brief's claim that `--tint` is the band wash was wrong, it styles the header nav hover; the plan file says so now).
- **C4 and S6.** Read-only lookup: live rows are one SIS section each (unique `section_id`), cross-listed partners are distinct ids (DATA C8 20817, STAT C8 20818), nothing names the partner, the course page filters by exact key. Pinned as #25 (tests and one contract sentence; approved).
- **C2.** #27, the live status sentence from the data branch's `status.json` on every page. The ux-copy pass found my brief's wording contradicted the neighbouring Spring 2027 line, so it names the project and the snapshot's term. Review: one Important (the smoke depended on GitHub answering); fix round 1: the smoke serves the URL from the fixture on every render and a 37th render aborts it, a harness test makes the fetch reject; scoped re-review: all addressed, no new breakage.
- **C3.** #26, the fallback ladder with every curve ("This curve: …, n = N." plus the fixed section rung); on the course page it sits in the headline card, since that chart draws every bucket. Review approved; one fix round pinned the placement, reworded `OTHER` to "the other departments", kept `n = N` on one line.
- **C5.** #30, `dept.html?subject=CODE`: the pool curve, every course of the subject by joins, the four states; the course table has three columns for phones. Review approved (four Minors: the static og tags, tied to the pretty-path decision; a docstring; the dept-mode marker); one fix round made the department link render whenever the subject is known, with a test.
- **C6.** Exporter half #28 (`titles.json` from the catalog: 1,728 of 2,050 Fall courses named, keys restricted to the index, `meta.titles_file`, `--catalog` and `--titles-only`; approved; one fix round for two DESIGN_A5 lines, a RUNBOOK sentence, plain CLI errors and the merge-order note) and matcher half #29 (a `title` layer after fuzzy, lazy-loaded only for a query with no digit and no subject token, whole-token title and surname match, `matchCourseAsync` for the pages while `matchCourse` stays synchronous; approved; one fix round added stop words).
- **C7.** The U8 copy audit ran as PR #31 on #22: eight findings, three fixed (the `OTHER` sentinel printed as a label in three places), five deferred (four sentences saying "the whole OTHER department", and the level tag inside the course page's h1, a structure change for you). Verified from the diff by the orchestrator rather than a review seat. The CSV download and L11 my-waitlists were not started.

**Rulings made on your behalf** (each with what it costs if wrong): (1) the CLAIMS re-measure done by the orchestrator through `claims-audit` rather than the scraper-ops agent, CLAIMS.md being orchestrator-only — none; (2) B1 done by the orchestrator with the reviewer afterwards — one unreviewed step, caught (Approved); (3) CLAIMS rows both sides re-measured take `main`'s newer result instead of sitting beside the old — one row to reword; (4) the B1 review's Minor fixed as one line on `site-v3` and the rebased branches re-rebased, the per-branch suite left to CI — a CI failure visible before any merge (none); (5) #11's CLAUDE.md line dropped in the rebase, the base's line already recording P4 — one sentence; (6) B2 rebases done by the orchestrator with one review at the end — an unreviewed intermediate step, covered; (7) B3 includes #11 although the plan's list omitted it, since you merge #11 before #17 — none; (8) the #17-versus-#11 data conflicts fixed on #17 as a Rebase-notes commit, B3 re-run — a second such commit; (9) #22's `--s1` shortfall on `--bg` and the ramp spacing left to you rather than reworked — one token value; (10) #23's VoiceOver risk not fixed in a CSS-only PR — one follow-up PR adding table roles; (11) the substituted plan-table query on #17 accepted — one fixture row; (12) the stale local `q1-q3-search` branch in a session-7 worktree reset to origin — nothing, the remote was right; (13) Minor fixes verified by the orchestrator from the diff and CI instead of a re-review seat, except C2's Important which got a scoped re-review — a two-line change to revert; (14) C2's wording change accepted — one sentence and its test strings; (15) C3's ladder placement in the headline card accepted — one block to move; (16) C6's exporter change handed to the analysis agent rather than edited by the orchestrator — one PR to close; (17) the exporter's leftover-file rule kept (an export without a catalog removes `titles.json`), with `run_analysis(catalog=)` on the next-session list — titles vanish after a weekly run until the next `make site-data`; (18) C5's three-column table and no filter link accepted — two columns to restore; (19) C7 limited to the U8 audit to keep your review queue at eleven new PRs — two items for a later session; (20) the C6 stop-word list and the exporter's `--titles-only` refusals decided by the agents within their briefs — a list to edit.

**Next session, in order.** (1) Read this note and merge in the order above; after #4 and #11, `pages.yml`, the curl loop, and check `live/latest.json` appears on the data branch after the next scrape run (`gh api repos/oliver139-chinesemole/berkeley-waitlist-odds/contents/live/latest.json?ref=data --jq .size`, about 160 KB). (2) Rebase the follow-up PRs onto `main` as their bases merge (#28 after #11 with its three noted conflicts; #24 and #29 after #17; #22 after #16; #23 and #25 after #20; #31 after #22). (3) Wire the catalog into `analysis.run` and the weekly workflow (`run_analysis(catalog=)`, `--catalog data-branch/catalog/<term>/catalog.json`) before the Spring 2027 switch, so `titles.json` survives a weekly run; then `make site-data` once with `--catalog` and commit `titles.json`. (4) Explicit table roles for the board and the Courses table (both use grid rows on phones). (5) The deferred Minors listed in each PR body; the Q4 cross-listed titles (23 courses). (6) Oct 4 term switch (RUNBOOK step 7; read `selected=` against the budget). (7) The remaining plan items: C7's CSV download and L11, then U1 and U2 with you present.

## What is left

**Backfill, in order (docs/dev/STATUS.md section 4 is the live list):**
1. Fall 2026 installed (PR #5); Spring 2026 pulled, cross-term read, level back to course (PR #6). Next: Oliver's decision on serving Spring 2026; B5 priority list before Oct 26.
2. Oliver's note to the Berkeleytime team was to be sent in parallel with the pull (his choice on 2026-09-20).
3. B5 (done 2026-09-24 in PR #9; the data-log row has the numbers): rebuild `config/priority_courses.txt` from reconstructed waitlist joins (top ~900 sections' courses) and log the new `priority_sha` before Oct 26. B6: fix the resume line, delete the two "Spring 2026" paragraphs (CLAIMS status, plan section 3), LICENSE, homepage, GoatCounter, move session scaffolding under `docs/dev/`.
4. A browser pass on the new page states (no browser in the sessions so far).

**Time-gated (nothing to build, just do on the date):**
1. **Sep 20 after 19:41 UTC, first check in a new session:** `git -C data-branch pull -q && python -m scraper.gaps --data-root ./data-branch --term-id 2268 --hours 24`. Expect about 48 runs, `share_gaps_le_45min` at or above 0.95, `median_missing_share` near 0 (node-id discovery finished with the 00:07Z run on Sep 20; the 00:37Z run observed all 1,287 selected sections, `n_missing` 0, in 1,341 s of the 1,380 s budget, so there is about 40 s of slack at one request per second: a bigger catalog or a slower site trims the shard tail first, never the priority list). If missing share stays around 0.14, the budget is short: a run selects about 917 priority plus about 240 shard sections at one request per second against a 1,380 s budget, so a bigger catalog or slower site trims the shard tail; see docs/RUNBOOK.md section 3. Then re-run the claims audit (rows 5 to 7). Issue #1 ("Scraper gap alert", opened by the 17:58Z monitor run on Sep 19) stays open until a monitor run passes; the chain dispatches the monitor after 15:00 UTC daily, so close it by hand on Sep 21 at the earliest (`gh issue close 1`).
2. **Oct 4 (Spring 2027 schedule publishes):** run RUNBOOK step 7: `gh variable set SCRAPE_TERM --body "Spring 2027"`, dispatch a forced baseline, confirm the pages report `data-term` 2272, add a `term_switch` row to docs/DATA_LOG.md. Discovery finds the new pages automatically over the following runs (about 6,000 nodes at 400 per run).
3. **Oct 12:** deadline for Spring collection to be running (it will be, if step 2 is done).
4. **Nov 9 onward (Phase 1 data exists):** first real `make analysis TERM=2272`, read `analysis/out/2272/report.md` and `profile.md`, write the two or three headline results in plain English into CLAIMS.md and the README, replace the simulated figure with `reports/figures_2272/hero.png`, check the site renders real estimates. Soft launch before Phase 2 (Nov 23) if the numbers hold up.
5. **Jan 2027 (Phase 2 closes):** refit, out-of-sample validation on Phase 2 joins, final numbers after Feb 10.
6. **Before mid-April 2027:** A7 ship (see below).

**Oliver's (cannot be done by the assistant):**
- Sunday two-minute check through Feb 14 (RUNBOOK section 4).
- Open the live site in a browser once and confirm the no-data state reads well (no browser was available in the sessions).
- (Superseded 2026-09-24 by the generated list in PR #9.) Review `config/priority_courses.txt` before Oct 26: only 99 of 478 waitlisted Fall sections matched the original list; UGBA, MEC ENG and PHYSED were added; ENGLISH, MUSIC, PBHLTH and HISTORY are the next largest gaps. Any edit changes `priority_sha`; log it in the data log.
- Optional sponsored request for the SIS Class API (a professor or the ASUC OCTO Berkeleytime team).
- A7: the r/berkeley post, the resume bullets ("Spring 2027 enrollment cycle", real section and snapshot counts, one headline result), pinning the repo on the GitHub profile, the private interview-prep page, LinkedIn.
- The design-critique, accessibility and UX-copy reviews the plan names as skills were done by hand; a human pass on the site before the soft launch is still worth doing.

**Engineering that could still be done now (not required):**
- The plan's `superpowers`, `data`, `design` and `humanizer` skills are not installed in this environment; their disciplines were applied by hand. Installing them is section 0 of the plan.
- Done in session 3: the site render test (above). Measured and closed without a code change: `catalog.json` was rewritten on every run only because the stuck watermark (data log, 20:37Z row) re-probed the same 400 node ids each run and stamped 169 entries with a new `probed_at`; that cost about 7.7 KB (gzip) per run next to a 15.8 KB Parquet delta. Ordinary fetches do not touch the catalog (a live section's entry changes only when its id, status, node id, course title or instructors change (the last two since Q7, 2026-09-24)), so after the catch-up the file changes only when something is learned.

## How to resume

```
cd /Users/oliverguo/berkeley-waitlist-odds && source .venv/bin/activate
git pull -q origin main
git -C data-branch pull -q || git clone -q --branch data --single-branch git@github-chinesemole:oliver139-chinesemole/berkeley-waitlist-odds.git data-branch
python -m pytest -q                                   # 320 tests, about 70 s; tests/test_site.py needs node (PATH or ~/.nvm)
python -m scraper.gaps --data-root ./data-branch --term-id 2268 --hours 24
gh run list -R oliver139-chinesemole/berkeley-waitlist-odds --workflow scrape.yml --limit 6
```

One more thing before resuming: on the evening of Sep 19 up to three Claude Code sessions were open on this same checkout (the second one found a commit it had not made; at 01:00Z on Sep 20 a third was writing an uncommitted backfill and backtest feature, `analysis/backfill.py` and `analysis/backtest.py`, which that session documents itself). Close other sessions before starting a new one, or at least run `git status` and `git log -3` first and never stage with `git add -A`.

## Decisions worth knowing before changing anything

- Only `scrape.yml` writes the `data` branch; only `analysis.yml` commits to `main` (site data and reports). Never commit to `data` by hand.
- Workflows dispatch each other with the built-in token on purpose; a push made by a workflow does not trigger other workflows.
- Flows are lower bounds; an enrolment gain with a non-empty waitlist counts as admits (FIFO); intervals overlapping a logged outage are censored; a `source_switch` or `schema` row in the data log is a point event, an `outage` row is a span.
- The site never shows simulated numbers; the validation figure is labelled as simulated everywhere it appears.
- Berkeley's registrar runs the last automatic waitlist process on Feb 5, 2027; nothing after it counts as queue movement.
