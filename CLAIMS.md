# Claims

Every number or capability that appears on the resume for this project, with the exact command that reproduces it. This table is the audit, the interview prep, and the source for README numbers (docs/dev/FINISH_PLAN_WAITLIST.md section 1.3). Re-run every row at the end of every step and before any resume update. If the `claims-audit` skill is installed, it does this; otherwise run each command by hand.

Rules:
- Result is what the command printed. Do not type a result you did not see.
- Date is the UTC date the Result was last produced. `-` means the check has never been run.
- If reality and the resume disagree, the resume changes, not the number.

Status on 2026-09-20 (session 4): the scraper is live on GitHub Actions against Fall 2026 (test term) on the self-dispatching chain; the analysis code has been exercised end to end on a 296-section pilot of Berkeleytime's Fall 2026 history and the full pull is running (rows below marked pilot are re-measured when it lands). Collection claims (30-minute snapshots, gap log, zero cost) refer only to this project's own Spring 2027 scraper; the backfilled Berkeleytime data is labelled `berkeleytime_history` and never counts toward them. The resume must name the Spring 2027 cycle (Oct 2026 to Feb 10, 2027) once that data exists; it is the only cycle this project's own scraper can capture.

Commands assume `source .venv/bin/activate` from the repo root, `export GH_REPO=oliver139-chinesemole/berkeley-waitlist-odds`, and a clone of the `data` branch at `./data-branch` (docs/RUNBOOK.md step 6). `<TERM_ID>` is `2268` for Fall 2026 test runs and `2272` for Spring 2027 (expected value; confirm on the first Spring 2027 run per docs/DESIGN_A2.md section 11).

| Claim | Check (exact command) | Result | Date |
| --- | --- | --- | --- |
| Resume: zero-cost pipeline (Python, GitHub Actions, Parquet) capturing 30-minute snapshots of enrollment and waitlist counts across sections through the Spring 2027 enrollment cycle. Pass: `share_gaps_le_45min >= 0.95` over the whole cycle and about 48 runs per day. | `python -m scraper.gaps --data-root ./data-branch --term-id 2272 --hours $(python -c "import datetime as d; print(max(1, int((d.datetime.now(d.timezone.utc)-d.datetime(2026,10,12,tzinfo=d.timezone.utc)).total_seconds()//3600)))")` | BLOCKED: Spring 2027 has no data yet: n_runs 0 over the 1 h window (term publishes Oct 4, Phase 1 opens Oct 26); Fall 2026 test data is under term 2268 | - |
| Resume: flow reconstruction separating waitlist admits, joins, and drops under documented FIFO assumptions. Pass: `docs/ASSUMPTIONS.md` exists and the synthetic-recovery test passes within its stated error. | `test -f docs/ASSUMPTIONS.md && python -m pytest tests/test_flows_synthetic.py -q -s` (prints the recovery table; copy the per-flow errors into the Result) | exit 0: 5 passed; docs/ASSUMPTIONS.md section 7 has the numbers. Synthetic recovery at 30-minute sampling: admits -8.6%, joins -3.6%, waitlist drops -0.7%, direct enrolments -4.0%, enrolled drops -9.2%; single-event intervals exact; central time-to-clear median abs error 25 min, bracket coverage 85% | 2026-09-19 |
| Resume: Kaplan-Meier and Cox proportional hazards, stratified by course, position, and enrollment phase. Pass: every figure and table rebuilds from raw Parquet with one command; output includes the PH assumption check and out-of-sample concordance, Brier score, and calibration. | `make analysis` (target defined in step A5) | exit 0 (`make analysis TERM=2268`, 13 runs on the data branch): panel 2,898 sections, observed share 0.429; flows 1,162 sections / 10,409 intervals, median interval 30.0 min, share censored 0.2218, admits 0, waitlist joins 0, waitlist drops 1, enrolled drops 60; cohort 1,974 rows per scenario, 0 clearing events, so the report notes "cohort too small for models" and KM/Cox/OOS are skipped (also noted: more than 2% of observed rows show enrolment above capacity). The same pipeline on simulated data produces all tables, figures and site JSON (tests/test_run.py). Real numbers after Oct 26. | 2026-09-19 |
| Resume tech line: GitHub Pages. Pass: the public lookup page returns 200 and reads its numbers from precomputed JSON under `site/data/`. | `curl -sS -o /dev/null -w '%{http_code}\n' https://oliver139-chinesemole.github.io/berkeley-waitlist-odds/` (JSON file name is fixed in step A6; add a second curl for it then) | 200 for the page, methodology.html, data/meta.json and data/courses.json (analysis.yml run 35477152275 committed site/data as 73a299c; pages.yml run 35477232784 served it). The served meta.json shows Fall 2026 test data: 973 sections, 1974 hypothetical joiners, 0 clearings, so the page renders its no-data state; rendered through tests/site/harness.js it reads "Snapshots so far: 973 sections, 1974 hypothetical joiners, 0 clearings" (before c4bed30 it read "[object Object] hypothetical joiners"). Real estimates after Spring 2027 waitlists start clearing | 2026-09-19 |
| Snapshot count per day. Target about 48; the monitor alerts below 40. | `python -m scraper.gaps --data-root ./data-branch --term-id <TERM_ID> --hours 24` and read `n_runs` | n_runs 13 over the 24 h ending 2026-09-19T23:48Z (2 manual runs, 3 cron runs, 8 chain runs); over the 4.5 h since the chain started at 19:41Z: n_runs 8, largest_gap_min 30.1, share_gaps_le_45min 1.0 | 2026-09-19 |
| Share of snapshot intervals at or under 45 minutes. Target 0.95 or higher. | `python -m scraper.gaps --data-root ./data-branch --term-id <TERM_ID> --hours 24 --fail-if-gap-min 90 --fail-if-runs-lt 40; echo "exit $?"` and read `share_gaps_le_45min` and `largest_gap_min` (exit 0 means both thresholds held) | exit 1: BREACH largest gap 320.5 min exceeds 90 min (02:21Z to 07:41Z, before the schedule fix); BREACH only 13 runs, fewer than 40; share_gaps_le_45min 0.5833 over 24 h, 1.0 over the 4.5 h since the chain started | 2026-09-19 |
| Sweep time per run, in seconds. Must stay under the 1200 s budget and stable across a week. | `git -C ./data-branch pull -q && python -c "import json; d=json.load(open('./data-branch/status.json')); print(d['last_run_at'], d['source'], d['scope'], d['n_observed'], d['sweep_seconds'])"` | 2026-09-20T00:37:41.969387+00:00 classes_site priority 1287 1340.886 (budget 1380 s since 19:15Z on Sep 19; first run after node-id discovery caught up: n_missing 0, about 40 s of slack. Earlier runs on Sep 19 spent about 400 s per run on discovery and left 150 to 250 shard-tail sections unattempted; the priority list was complete in every run) | 2026-09-20 |
| $0 recurring cost. Pass: repo is public (unmetered Actions minutes) and the billing page shows $0.00 for Actions and Storage for the month. | `gh repo view oliver139-chinesemole/berkeley-waitlist-odds --json visibility -q .visibility` prints `PUBLIC`; then open https://github.com/settings/billing and record the Actions and Storage charges | PUBLIC; billing page not checked (manual step for Oliver) | 2026-09-19 |
| Cross-check agreement with Berkeleytime on 20 sections. Pass: enrolled, waitlisted, and max agree exactly for all 20 fetched at the same time. | `python probe/crosscheck_berkeleytime.py --term "Fall 2026" --n 20` (writes docs/crosscheck_<date>.md; record "k of 20 agree") | 20 of 20 agree exactly (0 drift, 0 disagree, 0 excluded); docs/crosscheck_2026-09-19.md | 2026-09-19 |
| Test suite green, locally and in CI on main. | `python -m pytest -q` and `gh run list --workflow ci.yml --branch main --limit 1` | 356 passed locally on branch site-v3 (`python -m pytest`, 81.82 s, 2026-09-20 21:20Z, after the department-level default; tests/test_site.py needs node); CI on main last measured at the merge of PR #2: run 35483080491 success (322 tests); on branch site-v3 (PR #4, commit 72486f9) CI run 35531078835 success (`gh run view 35531078835`) | 2026-09-20 |

| Backfill (not a collection claim): Fall 2026 history pulled from Berkeleytime's public `GetEnrollment`, with its gaps measured. Pass: the pull is resumable, selection never reads today's counts, and `gap_report.csv` exists. | `make backfill-pilot` (300 sections) or `make backfill` (all), then read `backfill/2268/meta.json` and `backfill/2268/gap_report.csv` | Pilot 2026-09-20 01:34Z to 01:45Z: 300 selected, 296 histories (2 EWMBA sections have none), 20,266 segments, 37,978 panel rows, 19,970 crossings of which 2,679 wider than 180 min; histories 2026-03-22T12:27Z to 2026-09-20T01:30Z. Every section dark 2026-08-19T22:45Z to 2026-09-01T18:30Z (share_time_in_gap 1.0 on Aug 20 to 31), plus May 22, May 28, Jul 18 to 19; 18 of 183 days above 0.5, mean 0.105. Spring 2026 probe: 20 of 20 histories, 2025-10-27 to 2026-05-16. Full pull pending Oliver's note to Berkeleytime. | 2026-09-20 |
| Analysis on the backfilled term reproduces with one command and labels its source. Pass: `analysis/out/2268/report.md` says `berkeleytime_history` and the site meta carries `data_source`. | `make backfill-analysis TERM=2268` (pilot: `--site-dir` pointed at a scratch dir; `site/data` is written only after the full pull) | exit 0 on the 296-section pilot: flows 37,682 intervals (share censored 0.0711, admits 3,571, waitlist joins 4,674, waitlist drops 1,037); cohort 21,831 rows per scenario, 9,933 clearings; KM by bucket: 1-5 n 15,379 median 6.39 d P(clear by 14 d) 0.602; 6-15 n 4,814 median 26.1 d P(14 d) 0.40; 16-40 n 1,595 P(14 d) 0.21; 41+ n 43, 0 clearings. Cox: log_position coef -0.51 (HR 0.601, p 1.45e-16), wl_ratio -2.17, level=grad -1.01; PH fails for log_position and phase, stratified refit written. Out of sample at 14 d (fit before Jul 20): Cox AUC 0.631, Brier 0.504 vs 0.316 bucket baseline (pilot, pre-observability rule; see the next row). | 2026-09-20 |
| Backtest: frozen models scored on held-out joiners at their own horizons with IPCW, observability enforced, section-bootstrap intervals. Pass: `make backtest` writes `reports/backtest_<term>/*/metrics.csv` and the gain intervals are reported. | `make backtest TERM=2268` (pilot: `--out` pointed at a scratch dir) | Pilot, 296 sections. Grouped by course, 14 d (8,286 scored, 13,545 unobservable): Cox gain over bucket +0.0373 [+0.0235, +0.0526] (Brier 0.2100 vs 0.2473, AUC 0.736 vs 0.538); course identical to bucket; site -0.0145 [-0.0282, +0.0001]. Temporal at Jul 20, 14 d (2,938 scored, 9,739 unobservable): course -0.0143 [-0.0641, +0.0347]; site +0.0287 [-0.0340, +0.0819]; Cox -0.1958 [-0.2267, -0.1500], predicted 2 to 34% vs observed 54 to 77% by decile. Temporal deadline: 2,406 scored (all joiners after Sep 1), site +0.0853 [+0.0122, +0.1396], Cox -0.1840. 28 d and instruction horizons: 0 observable rows. Coverage at 14 d by phase: phase1 0.62, phase2 0.15, instruction 0.56, adjustment 0. | 2026-09-20 |

| Site v3 data: every number on the pages is read from files one command writes. Pass: `python -m analysis.export` prints the course count and writes `index.json`, `pooled.json`, `insights.json`, `meta.json` and `courses/<SUBJECT>.json`. | `python -m analysis.export --cohort analysis/out/2268/cohort_central.parquet --term-id 2268 --forecast-term 2272 --flows analysis/out/2268/flows.parquet --data-source berkeleytime_history --out <dir>` (or `make site-data`), then `du -sh <dir>/*` and `gzip -c <dir>/index.json | wc -c` | exit 0 on the full-pull cohort (493,364 rows) copied at 2026-09-20 18:26Z: `"courses": 2050`, `"events": 220196`, `"sections": 2923`, `"subject_files": 137`; 1 min 42 s; index.json 956 KB (89,205 bytes gzipped), pooled.json 96 KB (20,492 gzipped), insights.json 32 KB, courses/ 2.0 MB in 137 files. At the department-level default (`estimate_level` dept, the same evening): 1 min 20 s, index.json 1.0 MB (48,054 bytes gzipped), courses/ 792 KB in 137 files, pooled.json 96 KB. Re-measure after the install commit lands (STATUS item 1b). | 2026-09-20 |
| Site v3 renders in a real browser: every page at 390 by 844 and 1280 by 800, light and dark, no console error, no horizontal scroll, zero serious or critical axe-core violations. | `python tests/site/export_fixture.py /tmp/site && (python -m http.server 8000 --directory /tmp/site &) && node tests/site/smoke.mjs http://127.0.0.1:8000 shots` (CI: `.github/workflows/site-smoke.yml`; locally `NODE_PATH=/Users/oliverguo/paddleiq/node_modules`) | `36 renders, 0 with problems` (2026-09-20 20:35Z, served locally over the full-pull export with course DATA C8; again `36 renders, 0 with problems` at 21:25Z over the department-level export with course COMPSCI 61A; report.json: 0 renders with errors, 0 minor axe findings; 37 files in the output dir). Before the fixes of the same session the first run reported 4 problems: two `scrollable-region-focusable` and two `page scrolls horizontally` on the phone viewport. CI: site-smoke run 35531078834 success on site-v3 at 72486f9 against the synthetic fixture, artifact `site-screenshots-35531078834` 8,864,379 bytes (`gh run view 35531078834`) | 2026-09-20 |

## Rows to add later

- Every page of site v3 returns 200 after it lands: `for p in index courses course insights accuracy methodology about 404; do curl -sS -o /dev/null -w "$p %{http_code}\n" https://oliver139-chinesemole.github.io/berkeley-waitlist-odds/$p.html; done` plus `data/meta.json`, `data/index.json`, `data/pooled.json`, `data/insights.json` and `og.png`.
- Headline results in plain English (A5's last box): the Insights page composes them from `insights.json` (`all_by_bucket` read at 14 days, `median_days`, `by_level`); the CLAIMS row must quote the same file. Provisional, from the scratch export of the full-pull cohort on 2026-09-20 (re-measure after the install commit): within 14 days 62% at positions 1 to 5, 42% at 6 to 15, 25% at 16 to 40, 12% at 41 and up; half had a seat within 6.4 days at 1 to 5 and 25.7 days at 6 to 15; lower-division 48% against upper-division 41% at 6 to 15 (graduate 31%). Command: `python -c "import json; d=json.load(open('site/data/insights.json')); from analysis.export import read_curve; print({b: (read_curve(c['curve'], 14)[0], c['median_days']) for b, c in d['all_by_bucket'].items()}); print({l: g['6-15']['p'][1] for l, g in d['by_level'].items() if '6-15' in g})"`.

- Section count and row count for the resume bullet ("~X,000 sections, ~Y million rows"): the command is `python -m scraper.rebuild` output shape once step A4 lands. Do not fill the resume placeholders before then.
- Headline result (for example, median cleared position in a department) from step A5.
- Lookup tool usage (unique visitors from GoatCounter) from step A7. Screenshot the analytics page and keep it out of the repo.
